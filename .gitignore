# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/
mars_ls_env/

# Jupyter checkpoints
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# All runtime outputs (regeneratable — training results, inference, logs)
output/
v4_inference_output/
*_inference_output/

# Large zip archives of model outputs (use Git LFS or HuggingFace for weights)
trained_model_output.zip

# Dataset files (too large for git — place manually, see README)
data/
# *.tif
# *.TIF
*.npy

# Temporary / scratch
*.tmp
*.log
train_*.log
infer_*.log

# Model checkpoints (too large for git — use HuggingFace)
*.pt
trained_model_output.zip

# Keep JSON config/reports tracked (override the above for specific files)
# !trained_model_output/kfold_results_v4/norm_stats_v4.json
# !trained_model_output/kfold_results_v4/kfold_report_v4.json

submitted_training.ipynb
submitted_infer.ipynb
dual_swin_kfold_domain_adapted(1).zip